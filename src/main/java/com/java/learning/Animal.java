package com.java.learning;

// abstract class are classes that can not be instantiated (you can not create objects of these classes).
// These abstract classes can have abstract method and normal methods both. 
// Interface is also complete abstract class but in interface all methods need to be override by classes implementing this.
// in interface all methods are abstract method by default. 

public abstract class Animal {
	public abstract void animal_sound();
}
	